name: "Create cluster using KinD"
on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - main

jobs:
  kind:
    runs-on: ubuntu-latest
    steps:
      - name: First Step
        run: |
          echo "First!"
  echo:
    runs-on: ubuntu-latest
    needs: kind
    steps:
      - name: Echo
        run: |
          echo "Hello World After KinD"